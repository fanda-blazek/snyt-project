import type { SnytComponentMetadata } from "../../src/component-metadata.ts";

export const snytTabsPlan = {
  description: "Tabs primitive for switching between related panels.",
  name: "tabs",
  status: "planned",
  tagName: "snyt-tabs",
} as const satisfies SnytComponentMetadata;
