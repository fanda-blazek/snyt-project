# Autocomplete

Status: planned

Purpose: Suggest matching values while typing, with free-form input allowed.

Parts: root, input, trigger, popup, list, item.

Native base: `<input>`, `<datalist>`, combobox/listbox ARIA when custom popup is needed.
