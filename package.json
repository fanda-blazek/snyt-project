{
  "name": "snyt-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Headless UI primitives built on native Custom Elements.",
  "keywords": [
    "custom-elements",
    "headless-ui",
    "ui-primitives",
    "web-components"
  ],
  "type": "module",
  "scripts": {
    "ready": "vp check && vp run -r test && vp run -r build && playwright test",
    "dev": "vp run playground#dev",
    "dev:website": "vp run website#dev",
    "test:e2e": "playwright test",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.4.0"
}
